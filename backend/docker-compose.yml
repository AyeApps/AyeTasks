services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=AyeTasks
      - APP_ENV=development
      - PORT=8000
      - MONGODB_URL=mongodb://mongodb:27017/ayetasks
      - JWT_SECRET_KEY=super_secure_secret_key_minimum_32_characters_for_ayeapps_atelier
      - CORS_ORIGINS=["*"]
    depends_on:
      - mongodb
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  mongodb:
    image: mongo:7.0
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
